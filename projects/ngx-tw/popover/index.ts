export {
  PopoverDirective,
  type PopoverPosition,
  type PopoverScrollStrategy,
  type PopoverBackdrop,
  type PopoverTrigger,
} from './popover';
export { PopoverCloseDirective } from './popover-close';
export { PopoverTitleDirective } from './popover-title';
export {
  TW_POPOVER_DATA,
  TW_POPOVER_REF,
  /** @deprecated Use `TW_POPOVER_DATA` — same token instance. */
  POPOVER_DATA,
  /** @deprecated Use `TW_POPOVER_REF` — same token instance. */
  POPOVER_REF,
  type PopoverRef,
  type PopoverTemplateContext,
} from './popover-tokens';
